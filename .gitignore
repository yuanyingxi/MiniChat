# 操作系统与 IDE 特性文件
.DS_Store
desktop.ini
.idea/
*.iml
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Java / Maven 编译输出
**/target/
*.class

# Docker 协作核心隔离规则 (极为重要)
# 真实环境变量不进 git（example 模板可进）
.env

# 本地密钥文件（不进 git，example 模板会进）
**/application-secret.yml
**/application-secret.yaml

# 中间件持久化数据本地化：deploy/ 下独立 .gitignore 精确控制（/data/），
# 下面这些作为全仓兜底规则，拦截任何散落在业务模块下的 data/ 子目录
**/data/mysql/
**/data/redis/
**/data/elasticsearch/
**/data/rocketmq/

# 前端 Vue 编译输出
/node_modules/
/dist/

# 拦截所有目录下任何地方产生的日志文件及日志文件夹
*.log
**/logs/
/logs/
.claude/

.omo/
.workbuddy
